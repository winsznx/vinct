[package]
name = "vinct-adapter"
version = "0.1.0"
description = "VINCT vinct-adapter program"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib", "lib"]
name = "vinct_adapter"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build"]
anchor-debug = []
custom-heap = []
custom-panic = []

[dependencies]
anchor-lang = { version = "=1.0.2", features = ["init-if-needed"] }
sha2 = "=0.10.9"
ephemeral-rollups-sdk = { version = "=0.16.2", features = ["anchor"] }

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(target_os, values("solana"))'] }
