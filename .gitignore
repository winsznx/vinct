# Rust / Anchor
target/
**/*.rs.bk

# Node
node_modules/
.pnpm-store/
dist/
.next/
*.tsbuildinfo

# Project-pinned toolchains (reinstalled by scripts/bootstrap-toolchain.sh)
.toolchain/

# Local MagicBlock stack runtime state
magicblock-test-storage/
test-ledger/
mb-stack.log

# Secrets and keys — never commit
*.pem
*.key
**/id.json
**/*-keypair.json
.env
.env.*
!.env.example

# OS
.DS_Store

# Playwright output. Videos and traces are large and are regenerated by
# `pnpm proof:web`; the summary that matters is committed under artifacts/web/.
artifacts/web/test-results/
artifacts/web/report/
apps/web/dist/
